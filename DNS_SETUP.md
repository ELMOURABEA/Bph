# إعداد DNS Records لـ صيدليات البنداري (bendaryph.com)

مرحباً مصطفى! بناءً على خطتك لربط الدومين الخاص بك (الذي تم شراؤه من Google Domains) مع Vercel وخدمات البريد المؤسسي، إليك خطوات إعداد الـ DNS كاملة:

## 1. ربط الدومين بـ Vercel (الموقع الأساسي)

لجعل موقعك يعمل على `www.bendaryph.com` ونسخة الجذر `bendaryph.com`، قم بإضافة السجلات (Records) التالية في إعدادات DNS على Google Domains:

### A Record (لربط الدومين الأساسي بدون www)
* **النوع (Type):** `A`
* **الاسم/المضيف (Name/Host):** `@` (أو اتركها فارغة حسب واجهة Google)
* **القيمة (Value/Data):** `76.76.21.21` (هذا هو الآي بي الخاص بـ Vercel)
* **TTL:** `3600` (أو الافتراضي)

### CNAME Record (لربط www)
* **النوع (Type):** `CNAME`
* **الاسم/المضيف (Name/Host):** `www`
* **القيمة (Value/Data):** `cname.vercel-dns.com`
* **TTL:** `3600`

---

## 2. النطاق الفرعي للوحة التحكم (Admin)

لربط لوحة تحكم الإدارة بالرابط `admin.bendaryph.com`:
* **النوع (Type):** `CNAME`
* **الاسم/المضيف (Name/Host):** `admin`
* **القيمة (Value/Data):** `cname.vercel-dns.com` (إذا كانت اللوحة مستضافة أيضاً على Vercel كمشروع منفصل).

*(ملاحظة: لقد قمنا بالفعل بتحديث الكود في التطبيق ليوجه إلى الرابط الجديد للوحة الإدارة `https://admin.bendaryph.com` وسيتم تطبيق ذلك بمجرد النشر).*

---

## 3. إعداد البريد المؤسسي (Google Workspace لـ Gmail)

بما أن الدومين من Google Domains، فإن إعدادات Google Workspace غالبًا ما تكون تلقائية بنقرة واحدة. ولكن إذا احتجت لإضافتها يدوياً، فهذه هي الـ MX Records الضرورية:

| النوع (Type) | الاسم (Name) | القيمة (Value) | الأولوية (Priority) |
|---|---|---|---|
| MX | @ | smtp.google.com | 1 |
| TXT | @ | v=spf1 include:_spf.google.com ~all | - |

*(ملاحظة: السجل الأخير TXT خاص بـ SPF لمنع رسائل الإيميل من الذهاب للـ Spam).*

---

## 4. الخطوات المتبقية داخل Vercel
1. اذهب إلى لوحة تحكم Vercel.
2. ادخل إلى مشروعك -> **Settings** -> **Domains**.
3. أضف الدومين بشقيه: `bendaryph.com` و `www.bendaryph.com`.
4. إذا لم يقم Vercel بالتحقق من الدومين فوراً، انتظر من 15 دقيقة إلى بضعة ساعات لانتشار تحديثات الـ DNS.

بمجرد إتمام ذلك، سيعمل المتجر والتطبيق الخاص بك بالدومين الرسمي، مع البريد المؤسسي!
